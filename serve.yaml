apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  selector:
    app: nginx  # This matches the label defined in the Deployment
  ports:
    - protocol: TCP
      port: 8080      # Port exposed by the service
      targetPort: 8082  # Port on which NGINX container is listening
  type: LoadBalancer